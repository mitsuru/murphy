[package]
name = "murphy-reporting"
version = "0.1.0"
edition = "2024"

[dependencies]
murphy-core = { path = "../murphy-core" }
serde_json = "1"

[dev-dependencies]
criterion = { version = "0.8", default-features = false, features = [
    "cargo_bench_support",
] }

[[bench]]
name = "human_format"
harness = false
