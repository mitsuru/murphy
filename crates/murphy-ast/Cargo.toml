[package]
name = "murphy-ast"
version = "0.1.0"
edition = "2024"
description = "Owned, flat, parser-shaped, typed arena AST — Murphy's core AST representation (ADR 0037)."
build = "build.rs"

[dependencies]
sha2 = "0.10"
